<%= form_for(@answer_comment) do |f| %>
  <% if @answer_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_comment.errors.count, "error") %> prohibited this answer_comment from being saved:</h2>

      <ul>
      <% @answer_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<p>
<%= f.label :content %><br />
<%= f.text_field :content %>
</p>

<p>
<%= f.label :user_id %><br />
<%= f.number_field :user_id %>
</p>

<p>
<%= f.label :answer_id %><br />
<%= f.number_field :answer_id %>
</p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
